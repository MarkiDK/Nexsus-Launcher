// Opret __tests__ mappe med test filer
// server.test.js
describe('ServerManager', () => {
  test('should filter servers correctly', () => {
    const manager = new ServerManager();
    // Add test cases
  });
});
